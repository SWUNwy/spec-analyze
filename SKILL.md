---
name: spec-analyze
description: "需求分析与带注释方案输出。将模糊的产品需求转化为结构化、研发友好的方案文档。当需要进行需求分析、产品方案设计、功能拆解、竞品调研并输出可直接交付研发评审的 proposal/design/tasks 文档时使用。区别于通用分析工具：spec-analyze 的分析结果直接输出为带研发注释的规范文档格式。"
---

# Spec Analyze — 需求分析与带注释方案输出

将模糊的产品需求，通过多视角分析、压力测试、方案收敛，最终输出为**带研发注释**的 proposal / design / tasks 规范文档，让研发和 AI Agent 拿到文档即可开发。

<HARD-GATE>
在获得用户设计确认之前，不得调用任何实现 skill、编写代码或搭建项目。
</HARD-GATE>

---

## Step 0: 智能路由

在提问前先做三维评估：

| 维度 | 取值 |
|------|------|
| 讨论性质 | 纯业务 / 需求分析 / 技术设计 |
| 复杂度 | Lightweight / Standard / Full |
| 预期产出 | Insight Brief / Analysis Report / 带注释的方案文档 |

一句话确认路由：

> "这属于 [讨论性质] 类型，[复杂度] 复杂度。我会使用 [路径] 路径——[路径说明]。可以吗？"

### 三条路径

| 路径 | 流程 | 产出 |
|------|------|------|
| **Lightweight** | 快速提问 → 讨论 → 轻量输出 | Insight Brief |
| **Standard** | 2-3 角色视角 → 针对性问题 → 方案比对 → 报告 | Analysis Report |
| **Full** | 5 角色全视角 + 压力测试 + 方案收敛 + 设计呈现 → 带注释的规范文档 | 带注释的 proposal + design + tasks，然后接手到 `writing-plans` |

### Lightweight 升级门

Lightweight 路径在输出前自动评估：
1. 分析中是否涉及设计决策、需要编码实现？
2. 用户是否表达了实施意图（"做" / "开发" / "implement"）？
3. 结论是否涉及技术选型或架构变更？

任一为"是" → 提议："分析中已涉及实施需求，是否升级到 Standard 路径生成结构化输出？"

---

## Checklist

1. **快速评估** — 讨论性质、复杂度、预期产出
2. **路由确认** — 推荐路径，等待用户确认
3. **视觉伴侣邀请** — 如果涉及视觉内容，独立消息邀请（见 Visual Companion 章节）
4. **上下文探索** — 查阅项目文件、文档、最近提交；按需提议 web research（见 Web Research 章节）。**→ 检查 G1 门禁**
5. **多角色提问** — 一次一个问题，按激活的角色视角引导（见 多角色驱动提问 章节）[skip Lightweight]
6. **发散阶段** — 场景压力测试 + what-if 探针（见 压力测试章节）[skip Lightweight]
7. **收敛阶段** — 2-3 方案带 trade-off 对比 + 决策记录。**→ 检查 G2 门禁**
8. **设计呈现** — 分节呈现，逐节获取用户批准。**→ 批准后检查 G3 门禁**
9. **输出生成** — 按路径产出对应文档。Full 路径使用带注释的模板格式（见 输出模板 章节）。**Lightweight 路径先评估升级门**
10. **质量自检** — 运行路径对应的 DoD 检查清单
11. **用户审阅** — 输出文档让用户确认
12. **交接** — Full 路径 → 调用 `writing-plans`；其他 → 结束

---

## 质量门禁与 Definition of Done

### 路径 → 门禁映射

| 门禁 | 位置 | Lightweight | Standard | Full |
|------|------|:-----------:|:--------:|:----:|
| G1: 上下文完备 | 步骤 4 → 5 | 跳过 | 需要 | 需要 |
| G2: 收敛完成 | 步骤 7 → 8 | 跳过 | 需要 | 需要 |
| G3: 输出准备 | 步骤 8 → 9 | 跳过 | 需要 | 需要 |
| G4: 自检完成 | 步骤 10 → 11 | 需要 | 需要 | 需要 |

### G1: 上下文完备

- [ ] 上下文边界清晰：scope 内 / scope 外已明确
- [ ] 项目文件/文档已查阅
- [ ] Web research（如触发）已完成
- [ ] 对当前状态的理解足以进入深入分析

### G2: 收敛完成

- [ ] 至少 2-3 个方案已对比，各列明 trade-off
- [ ] Architecture Cleanliness 四维度已评估（模式一致性 / 职责分离 / 最小变更 / 抗补丁性）
- [ ] 每个决策记录了 rationale + 被拒绝的替代方案
- [ ] 范围已锁定——明确声明**不包含**的内容

### G3: 输出准备（Full 路径特有检查）

- [ ] proposal 模板中的注释列已确认有足够数据填充
- [ ] design 模板中的交互组件已识别
- [ ] 输出模板已读取，所有章节有分析数据支撑
- [ ] 文件输出路径已确定

### G4: 自检完成

- [ ] 无占位文本（`{占位符}` 均已替换）
- [ ] 所有声明已区分 Fact / Inference / Hypothesis
- [ ] 没有 scope creep
- [ ] Full 路径：提交的注释符合「质量自检清单」的全部标准
- [ ] Full 路径：proposal / design / tasks 三文档的引用链一致（tasks 引用的章节在 design 中存在）

### 失败处理

- 任一 DoD 不满足 → 回溯对应步骤补齐
- 用户否决输出 → 回到设计呈现重新迭代
- 同一门禁连续阻塞 2 次 → 暂停，评估路径选择是否恰当

---

## 二、分析过程

### 2.1 上下文探索

- 先查阅项目状态（文件、文档、最近提交）
- 如果需求描述了多个独立子系统 → 立即标记，帮助拆分为子项目
- 每个消息只问一个问题
- 优先用选择题，开放题也可以
- 一个问题需要展开时拆成多个子问题

### 2.2 设计隔离与清晰度

在收敛阶段呈现设计时：
- 每个模块有单一职责、清晰接口、可独立测试
- 每个模块回答：做什么、怎么用、依赖什么
- 文件过大 = 职责过多的信号 → 在设计中标记

### 2.3 工作于现有代码库

- 先探索现有结构，再提议变更
- 如果现有代码影响当前工作 → 包含针对性改进
- 不提议无关的重构

### 2.4 多角色驱动提问

根据激活的角色视角提问。**每个角色至少问 1-2 个问题。**

| 角色 | 关注点 | 典型问题 |
|------|--------|----------|
| **产品策略师** | 价值定位、用户画像、差异化 | "这个功能解决什么用户的什么问题？" "不做这个会怎样？" |
| **增长与市场分析师** | 竞争格局、市场趋势、商业模式 | "行业里有没有类似功能？" "上线后怎么衡量成功？" |
| **用户代言人** | 用户旅程、痛点、可用性 | "用户第一次用会卡在哪？" "操作失败时用户怎么办？" |
| **系统架构师** | 可扩展性、可行性、数据模型 | "这个字段的数据从哪来？" "API 如果超时/报错怎么处理？" |
| **风险挑战者** | 极端边界、失败模式、盲点 | "如果用户输入恶意数据呢？" "如果这个假设不成立呢？" |

切换角色视角时说明来源：

> "从增长角度来看——用户今天怎么发现这个功能的？"
> "风险挑战者要问——如果这个假设是错的会怎样？"

### 2.5 发散阶段: 压力测试

从以下场景库中选择 2-3 个相关场景进行 what-if 探测：

| 场景类型 | 探测问题 |
|----------|----------|
| **数据异常** | 如果 API 返回空数据 / 格式错误 / 超时？ |
| **用户行为** | 如果用户快速重复操作 / 输入超长文本 / 输入特殊字符？ |
| **边界条件** | 如果断网 / 弱网 / 权限不足 / 浏览器缩放 200%？ |
| **并发冲突** | 如果两个 Tab 同时操作同一资源？ |
| **依赖失效** | 如果第三方服务 / 基础组件挂了？ |
| **数据一致性** | 如果本地缓存和服务器数据不一致？ |

### 2.6 收敛阶段: 方案比对

1. 提出 2-3 个方案，带显式 trade-off（推荐方案放首位）
2. 每个方案在 Architecture Cleanliness 四维度上评估
3. 记录每个决策的 rationale + 被拒绝的方案
4. 锁定范围——明确声明**不包含**的内容

### 2.7 Agent 角色动态

| 阶段 | 角色 | 行为 |
|------|------|------|
| 早期探索 | 引导者 (Socratic) | 提问式探索、帮助理清思路 |
| 发散 | 挑战者 | 推边界、问"what if"、暴露盲点 |
| 收敛 | 顾问 | 推荐方案、排序、做 trade-off 决策 |
| 设计呈现 | 协作者 | 分节呈现、按反馈迭代 |

---

## 三、注释框架

分析完成后，Full 路径的文档输出使用以下注释体系。

### 3.1 三层注释等级

| 等级 | 适用场景 | 包含字段 |
|------|----------|----------|
| **L1 核心** | 简单交互（hover tooltip、静态展示） | trigger / behavior / dismiss |
| **L2 标准** | 复杂交互（弹窗、下拉、表单联动） | L1 + placement / style / state / timing |
| **L3 完整** | 高精度/全局组件（DateShow、Modal、Table） | L2 + accessibility / responsive / i18n |

### 3.2 字段定义

```
L1 共用
──────────────────────────────────────
trigger   触发条件      hover / click / focus / scroll
behavior  行为描述      弹出 Tooltip / 展开下拉 / 提交
dismiss   消失条件      mouse leave / click outside / Esc

L2 增加
──────────────────────────────────────
placement 出现位置      top / bottom / topRight / center
style     视觉细节      color / spacing / font / z-index / content type
state     各状态行为    normal / empty / loading / error / overflow
timing    动画与延迟    200ms 淡入 / 100ms 淡出 / 300ms debounce

L3 增加
──────────────────────────────────────
accessibility 无障碍    Tab 聚焦 / Enter 触发 / Esc 关闭 / aria-label
responsive 响应式       touch fallback / 小屏适配
i18n      国际化        是否需要翻译
```

#### State 编写规范

编写 state 时需覆盖两个视角：

| 视角 | 要求 | 示例 |
|------|------|------|
| **研发视角** | 描述组件在该状态下的行为表现 | `submitting: 按钮 loading + disabled, 文字'登录中...'` |
| **测试视角** | 描述从触发到状态呈现的完整路径 | `error: 输入无效邮箱后 blur → 红色边框 + '邮箱格式不正确'` |

**state 必须覆盖**: normal + 至少 2 个非正常状态（loading / error / empty / countdown）

### 3.3 使用原则

- **L1 默认**：所有交互注释从 L1 开始
- **按需升级**：L1 不够研发理解才升级到 L2
- **L3 留给全局组件**：仅多处复用的通用组件才需要
- **不重复已知信息**：如 Ant Design 组件的默认行为不需要写进注释

### 3.4 角色 ↔ 注释字段映射表

| 角色 | 关注字段 | 关注原因 |
|------|----------|----------|
| 研发 | trigger, behavior, dismiss, state（边界） | 知道触发条件、实现行为逻辑、处理边界 |
| 测试 | state（所有分支）, trigger, dismiss | 状态转化可转化为测试用例 |
| UI | style, placement, timing | 视觉细节、位置、动画 |

**多角色归属说明**：behavior 字段同时服务研发和测试（研·测），state 字段同时服务研发和测试（研·测），trigger/dismiss 主要服务研发，兼顾测试。

---

## 四、输出模板

Full 路径的文档输出使用以下带注释的模板格式。

### 4.1 proposal.md 模板

```markdown
# 方案文档 — {R0XX-需求名称}

> **需求编号**: R0XX
> **创建日期**: YYYY-MM-DD

## 一、需求概述

### 1.1 背景
### 1.2 目标
### 1.3 范围
- **包含**: [功能点列表]
- **不包含**: [排除列表]

---

## 二、功能需求

| ID | 需求描述 | 优先级 | 验收标准 | 数据说明 | 交互说明 | UI文本说明 |
|----|----------|--------|----------|----------|----------|-----------|
| F001 | [描述] | P0 | [条件] | [字段/格式/边界] | [L等级+简述] | [文案/标签] |

**三列填写指南:**

**数据说明列** — 必须包含：API 来源 + 格式规则 + 边界值。示例：
```
❌ 差: email: 邮箱格式校验
✅ 好: email: string, 必填, 邮箱格式 (含@和域名, 最长50字符), 空值→"请输入邮箱"
❌ 差: POST /api/auth/login
✅ 好: POST /api/auth/login, body: {email: string, password: string}, 返回: {token: string}
```

**交互说明列** — 必须包含：交互等级 + 一句话行为。示例：
```
✅ L2: 表单输入 → blur 单项校验 → submit 全量校验 → API → 成功跳转/失败Toast
✅ L1: click Tab 切换表单，重置校验状态
```

**UI文本说明列** — 必须包含：所有可见文本的具体文案。示例：
```
✅ placeholder: "请输入邮箱"; submit: "登录"; 格式错误→"邮箱格式不正确"
```

---

## 三、非功能需求（保持不变）

| 类型 | 要求 | 验收方法 |
|------|------|----------|

## 四、技术依赖（保持不变）

| 依赖项 | 来源 | 状态 |
|--------|------|------|
```

#### 填写示例（以登录页面的邮箱输入框为例）

```
| F001 | 邮箱密码登录 | P0 | 输入邮箱+密码→登录成功跳转首页 | email: string, 必填, 邮箱格式(含@和域名, 最长50); password: string, 必填, 最少6位, 最长32位; POST /api/auth/login body: {email, password} 返回 {token} | L2: input→blur单项校验, click登录→全量校验→API→成功存token跳转/失败Toast | placeholder: "请输入邮箱", "请输入密码"; 格式错误: "邮箱格式不正确","密码至少需要6位"; 提交按钮: "登录", loading时"登录中..." |
```

### 4.2 design.md 模板

```markdown
# 设计文档 — {R0XX-需求名称}

## 一、设计概述

## 二、系统架构

## 三、接口设计

| 接口 | 方法 | 参数 | 返回 | 错误场景 |
|------|------|------|------|----------|

## 四、数据模型

## 五、组件设计

### 5.1 {组件名}

| 组件名称 | 职责 | Props | State |
|----------|------|-------|-------|
| {Name} | {职责} | {Props} | {State} |

#### 交互注释 @{组件名} {L等级}

```
字段名:   字段值
```

#### 交互注释 @EmailPasswordForm L2（示例）

```
[研]   trigger:   input → blur 触发单项校验
                  click "登录" → 触发全量校验 + API 调用
[研·测] behavior:  blur: 校验单字段, 错误→红色边框+红色提示文字
                  submit: 全量校验→通过→POST /api/auth/login
                  → 成功: localStorage存token → 跳转首页
                  → 失败: Toast 显示后端 m 字段
[UI]   style:     圆角 4px, 高度 40px; 焦点边框高亮
[测]   state:     normal: 空表单; focused: 聚焦高亮;
                  error: 红色边框+提示文字; submitting: 按钮loading + disabled "登录中..."
[研]   dismiss:   成功→跳转; 失败→恢复normal
```

#### 交互注释 @SmsCodeBtn L2（示例）

```
[研]   trigger:   click "获取验证码"
[研·测] behavior:  校验手机号→通过→POST /api/auth/sms-code
                  → 成功: 开始60s倒计时, Toast "验证码已发送"
                  → 失败: Toast 显示错误
[UI]   style:     倒计时期间置灰 disabled, 按钮文字每秒更新
[测]   state:     normal: "获取验证码"; countdown: "58s后重新获取"; disabled: true
[UI]   timing:    倒计时每秒更新, 60s后恢复
[研]   dismiss:   倒计时归零→恢复normal
```

## 六、错误处理

| 错误类型 | 场景 | 处理方式 |
|----------|------|----------|

## 七、附录：字段说明表

| 所属模块 | 字段 | UI标签 | 格式约束 | 空值策略 | 数据来源 |
|----------|------|--------|----------|----------|----------|
```

### 4.3 tasks.md 模板

```markdown
# 任务清单 — {R0XX-需求名称}

## 一、任务列表

| 任务ID | 任务描述 | 预估工时 | 优先级 |
|--------|----------|----------|--------|
| T001 | [描述] | [工时] | P0 |

## 二、任务步骤

### T001: {任务描述}

> **注释参考:**
> - 交互注释 → design.md §{章节} @{组件名}
> - 字段文案 → design.md 附录「字段说明表」
> - 数据来源 → design.md §{章节}

1. {步骤1}
2. {步骤2}
...

## 三、依赖关系
```

---

## 五、全链路工作流

```
产品需求（自然语言）
   │
   ├── 1. spec-analyze 分析
   │       ├── 路由评估 → 路径选择
   │       ├── 上下文探索（项目文件/代码/文档）
   │       ├── 多角色提问（收敛需求）
   │       ├── 压力测试（识别边界和风险）
   │       └── 方案收敛 + 设计呈现
   │
   ├── 2. 输出生成（Full 路径）
   │       ├── proposal.md（功能需求表带三列注释）
   │       ├── design.md（组件设计带交互注释块 + 字段说明表）
   │       └── tasks.md（任务步骤带注释引用）
   │
   ├── 3. 需求评审
   │       ├── 产品经理 → F00X 描述列 + 验收标准
   │       ├── 研发 → 数据说明（格式约束） + 交互注释块 + 字段说明表
   │       ├── 测试 → 交互注释 state + 错误处理 + 边界值
   │       └── UI → style 颜色/间距 + responsive + 暗提示文案
   │
   ├── 4. Agent 开发
   │       ├── 读 tasks → 看注释引用
   │       ├── 跳读 design.md 交互注释块
   │       └── 按注释行为实现代码
   │
   └── 5. 代码产出
           ├── 字段文案 → placeholder / label / error text
           ├── 格式约束 → regex / length / required
           ├── 交互行为 → 与 design 注释一致
           └── 边界处理 → 与注释 state 一致
```

---

## 六、质量自检清单

每项自检必须逐条确认。任何一项不满足 → 回溯修正。

### proposal 自检

- [ ] 每个 F00X 的三列注释都已填写
- [ ] **数据说明标注了字段来源 + 格式约束 + 边界值**
  - 字段来源: API 路径? body 参数? 返回字段?
  - 格式约束: 数据类型? 长度限制? 正则? 枚举值?
  - 边界值: 空值策略? 默认值? 超长处理?
- [ ] **交互说明标注了等级（L1/L2/L3）+ 一句话行为**
- [ ] **UI文本包含所有可见文案: placeholder / label / error / tooltip**
- [ ] 交互等级选择合理（无过度注释: 简单交互不标 L2+）

### design 自检

- [ ] 每个交互组件都有交互注释块
- [ ] 注释块中的字段按等级选择完整（L1 至少触发/行为/消失）
- [ ] **state 覆盖了以下关键状态: normal + error + empty/loading（至少 2 个）**
- [ ] **错误处理章节已填写: 网络错误 / 校验失败 / 业务错误 三类都已覆盖**
- [ ] **字段说明表包含: 格式约束列（防 proposal 和 design 信息脱节）**
- [ ] L3 组件补充了 accessibility 和 responsive

### tasks 自检

- [ ] 每个实现任务包含注释引用段
- [ ] 引用段指向正确的 design.md 章节（章节号 + 组件名）
- [ ] 引用段包含组件名
- [ ] 所有被引用的 design.md 章节确实存在（cross-doc 一致性）

### cross-doc 一致性专项检查

- [ ] **proposal 的数据说明列 ↔ design 的接口/字段设计**: 字段名一致
- [ ] **proposal 的 UI文本说明列 ↔ design 的字段说明表**: 文案一致
- [ ] **proposal 的 F00X ID ↔ tasks 中引用的功能点**: 覆盖完整
- [ ] **tasks 的注释引用 ↘ design.md 中的章节**: 一一对应存在

---

## 七、评审角色检查清单

此清单用于需求评审环节，每个角色逐项确认。

### 研发视角评审

- [ ] 每个字段的格式约束明确（类型/长度/正则/枚举）         ← 来自交互注释 @{组件} behavior
- [ ] 每个字段的空值/边界策略明确                           ← 来自交互注释 @{组件} state
- [ ] 交互注释的 state 覆盖了 loading / error / empty        ← 来自交互注释 @{组件} state
- [ ] API 的请求/响应结构完整                               ← 来自交互注释 @{组件} behavior
- [ ] 错误处理的触发条件和表现明确                           ← 来自交互注释 @{组件} trigger + state

### 测试视角评审

- [ ] 交互注释的 state 可转化为测试用例（normal / loading / error / empty）  ← 来自交互注释 @{组件} state
- [ ] 错误处理章节覆盖了：网络异常 + 数据异常 + 业务异常                     ← 来自交互注释 @{组件} state + behavior
- [ ] 每个输入字段的边界值有定义（空/超长/特殊字符）                         ← 来自交互注释 @{组件} state
- [ ] 用户操作的触发条件可测试（blur / click / Enter）                        ← 来自交互注释 @{组件} trigger
- [ ] 流程有明确的"成功"和"失败"判定条件                                     ← 来自交互注释 @{组件} dismiss + state

### UI 视角评审

- [ ] 所有可见文案都已定义（placeholder / label / error / tooltip / button text）  ← 来自交互注释 @{组件} state
- [ ] 颜色/间距/圆角有标注                                                          ← 来自交互注释 @{组件} style
- [ ] 加载/空/错误三种状态的视觉表现有描述                                          ← 来自交互注释 @{组件} state
- [ ] 响应式/移动端行为有定义（如涉及）                                              ← 来自交互注释 @{组件} responsive
- [ ] 动效时长有标注（如涉及）                                                      ← 来自交互注释 @{组件} timing

---

## 八、Web Research

当讨论涉及以下主题时，提议做 web research：
1. 竞争基准 / 行业最佳实践
2. 市场趋势 / 用户行为数据
3. 技术评估（性能/社区/风险对比）
4. 合规需求（GDPR 等）

先请求许可：

> "这涉及 [主题]，我建议查一下 [内容]。需要我做个快速搜索吗？"

---

## 九、Visual Companion

当涉及可视化内容（架构图、页面布局、流程图表）时，提供浏览器伴侣。

**触发条件**：在 Full 路径的设计呈现阶段，或当文字描述不足以表达布局/流程时。

**邀请方式**（独立消息，不含其他内容）：

> "有些内容用浏览器展示会更直观——我可以画架构图、流程图或页面布局。要试试吗？（需要打开本地 URL）"

---

## 十、决策记录

每个关键决策记录：
- 决策了什么
- 考虑了哪些替代方案
- 为什么选这个方案
- 为什么拒绝其他方案

---

## 十一、关键原则

- **一次一个问题** — 不一次性抛多个问题
- **选择题优先** — 比开放题更容易回答
- **YAGNI** — 从所有设计中移除不必要的功能
- **先发散再收敛** — 先拓宽思路再收窄
- **增量验证** — 分节呈现设计，逐节获取批准
- **记录决策** — 不只记录结论，还要记录"为什么"
